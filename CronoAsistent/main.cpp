#include <iostream>
#include <fstream>
#include "dataextract.h"

using namespace std;

int main(void)
{
    //Mat_Registr();

    int NumMaterias=0;
    cout<<"Ingrese el numero de materias a registrar.";
    cin>>NumMaterias;
    char *RegistroMat=new char[NumMaterias];
    for (int var = 0; var < ; ++var) {

    }
}
